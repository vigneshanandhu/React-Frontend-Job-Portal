services:
  - type: web
    name: react-frontend-job-portal
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /
        destination: /index.html